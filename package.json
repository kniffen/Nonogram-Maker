{
  "name": "nonogram-maker",
  "version": "1.0.2",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx parcel src/index.html --out-dir dev",
    "build": "npx parcel build src/index.html --no-source-maps --out-dir ."
  },
  "keywords": [
    "nonogram"
  ],
  "author": "Kniffen",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
